# PBR 贴图助手 v2.5.1

在 Blender Shader Editor 里一键搞定贴图连线、重命名、清理、压缩、批量导入。把繁琐的节点操作交给插件。

## 解决的问题

拿到一堆贴图手动往 Principled BSDF 上拖、调色彩空间、删无用节点——一套材质做下来光连线就耗掉不少时间。多人协作时命名不统一，交接后还得逐个改。

这个插件根据贴图后缀自动识别通道、连线、修正色彩空间。支持批量导入外部贴图、压缩贴图体积、材质和贴图命名互相对齐。

## 基本信息

| 项目 | 内容 |
| :--- | :--- |
| 插件版本 | 2.5.1 |
| Blender 支持 | 3.4.0+ |
| 入口 | Shader Editor > 侧栏（N）> PBR Tool |

## 核心功能

### 🔧 贴图连接

**自动连接贴图** — 识别贴图后缀，接到 Principled BSDF 对应输入口。颜色通道自动设 sRGB，数据通道（法线、粗糙度等）设 Non-Color。如果工程里已有 Normal Map、Separate Color、MixRGB 等中间节点，会优先复用。

支持的复合图逻辑：
- `ORM` / `ARM`：R=AO，G=Roughness，B=Metallic
- `RMA`：R=Roughness，G=Metallic
- 独立 `AO`：通过乘法叠加到底色链路

**清除无用节点** — 删掉未连接到最终输出的孤立节点。

**重置材质节点** — 材质还原为基础白板，只保留 Principled BSDF 和 Material Output。

### 🏷️ 命名规则

**根据贴图重命名材质** — 从已连接贴图提取主名称，重命名材质。例如 `T_Prop_01_d` → `M_Prop_01`。重名时 Blender 自动追加 `.001`。

**根据材质球重命名贴图** — 由材质名推导贴图名（`M_xxx` → `T_xxx`），后缀从节点连接推断。默认只改 Blender 内部 Image 名称，不动磁盘文件，防止路径断链。

后缀推断规则：

| 推断后缀 | 连接含义 |
| :--- | :--- |
| `_ORM` | 经 Separate Color 后，绿接 Roughness、蓝接 Metallic。即使红仍接 glTF Occlusion，也按 ORM 整张贴图命名 |
| `_AO` | 仅红通道经 Separate Color 接外置 Occlusion，且未同时满足上述 ORM 的绿+蓝连接 |
| `_Alpha` | 连接至 Principled BSDF 的 Alpha 输入 |
| `_D` / `_N` / `_R` / `_M` | 分别对应 Base Color、Normal（经 Normal Map）、Roughness、Metallic 直连链路 |

多用途冲突时优先级：`_ORM` → `_AO` → `_N` → `_D` → `_Alpha` → `_R` → `_M`。

### 🖼️ 图像预览

在节点树中选中图像纹理节点，面板显示预览和关键属性：分辨率、通道类型、位深、色彩空间。可以直接在面板里改色彩空间和 Alpha 模式。

### 📦 图像压缩

**执行处理** — 按范围缩放贴图，支持跨中间节点追溯（Separate Color、Normal Map 等）找到实际贴图。

压缩参数：
- `压缩比例`：例如 0.5 = 缩到原始尺寸 50%
- `最小分辨率`：防止缩得过小
- `格式转换`：JPEG / PNG / TGA / TIFF / BMP / EXR

**打包导出贴图** — 把已连接贴图导出到指定目录。支持所选和全场景两个范围。导出后可点 `打开刚才导出的文件夹` 快速回看。

### 📥 外部批量导入

点 `基于名称匹配文件夹贴图`，插件按材质名推导贴图前缀并在目录中匹配文件：
- `M_Weapon` → 匹配 `T_Weapon_*`
- `m_weapon` → 匹配 `T_weapon_*`（不区分大小写）
- `Weapon` → 匹配 `T_Weapon_*`

## 贴图后缀速查

| 通道 | 可识别后缀 |
| :--- | :--- |
| Base Color | `_d`、`_bc`、`_basecolor`、`_albedo`、`_diff`、`_color` |
| Normal | `_n`、`_nor`、`_normal`、`_nrm` |
| Roughness | `_r`、`_roughness`、`_rough`、`_rgh` |
| Metallic | `_m`、`_metallic`、`_metal`、`_met` |
| AO | `_ao`、`_ambientocclusion`、`_occlusion` |
| Alpha | `_alpha`、`_opacity`、`_a` |
| ORM/ARM | `_orm`、`_orm_0`、`_orm_1`…（`_orm_` + 数字）、`_arm`、`_ao_r_m` |
| RMA | `_rma` |

识别时不区分大小写。交付建议统一输出大写后缀（`_D`、`_N`、`_R`、`_ORM`、`_AO`）。

## 常用流程

**乱节点工程整理**：选中模型 → 清除无用节点 → 自动连接贴图 → 根据贴图重命名材质。

**批量导入烘焙贴图**：导入模型 → 选中目标物体 → 打开外部批量导入 → 基于名称匹配文件夹贴图 → 选贴图目录。

**交付前压缩**：确认自动打包资源已开 → 设压缩比例和目标格式 → 执行处理 → 打包导出贴图。

**统一命名**：选命名范围 → 根据贴图重命名材质 或 根据材质球重命名贴图 → 检查结果。

## FAQ

**Q1：自动连接贴图有些图没反应？**
只对能识别后缀或可追溯到有效通道的图生效。插件不会猜测未知用途的贴图，安全优先。

**Q2：清除无用节点删了还能恢复吗？**
可以 Ctrl+Z 撤销。它只删未连到输出链路的节点。

**Q3：执行压缩前为什么要开启自动打包资源？**
关闭时可能直接影响磁盘原图。插件强制安全模式，必须先打包再处理，避免误改原文件。

**Q4：打包导出贴图能 Ctrl+Z 撤销吗？**
不能。文件系统写入不在 Blender 撤销栈里。导出前确认目录。

**Q5：根据材质球重命名贴图会不会动磁盘文件？**
默认只改 Blender 内部 Image 数据块名，不动磁盘路径。开启同步重命名才会改磁盘。

**Q6：贴图连接里的"全部"和导出里的"全场景"范围一样吗？**
不一样。贴图连接中的"全部"= 所选物体材质，导出中的"全场景"= `bpy.data.materials`。两处定义不同。
